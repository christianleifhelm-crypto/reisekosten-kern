{
  "name": "kern-aussendienst",
  "version": "1.0.0",
  "private": true,
  "description": "Kern Drucklufttechnik – Zeiterfassung, Reisekosten, Kundendatenbank (Android)",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "build:apk": "cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
